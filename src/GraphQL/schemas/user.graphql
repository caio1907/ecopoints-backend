input UserInput {
  firstName: String!
  lastName: String!
  email: String!
  password: String!
}

type User {
  firstName: String!
  lastName: String!
  email: String!
}

type Query {
  me: User
}

type Mutation {
  createUser(user: UserInput): User
}
